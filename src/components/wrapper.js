// import { BlueBaseApp } from '@bluebase/core';
import React from 'react';

// const componentsMap = {
//   ...baseComponents,
//   /* your custom components */,
// }

export default ({ children }) => {
	return children;
	// return <BlueBaseApp>{children}</BlueBaseApp>;
};

// export default theme(themeConfig)(Theme);
